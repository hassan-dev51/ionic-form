import { EnrollmentPipe } from './enrollment.pipe';

describe('EnrollmentPipe', () => {
  it('create an instance', () => {
    const pipe = new EnrollmentPipe();
    expect(pipe).toBeTruthy();
  });
});
